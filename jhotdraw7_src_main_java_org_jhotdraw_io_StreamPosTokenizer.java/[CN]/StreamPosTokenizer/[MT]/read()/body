{
  int data;
  if (unread.size() > 0) {
    data=unread.lastElement().intValue();
    unread.removeElementAt(unread.size() - 1);
  }
 else {
    data=reader.read();
  }
  if (data != -1) {
    readpos++;
  }
  return data;
}
