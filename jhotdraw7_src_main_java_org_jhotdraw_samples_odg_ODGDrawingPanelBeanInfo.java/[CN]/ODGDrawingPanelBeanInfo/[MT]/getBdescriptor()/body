{
  if (beanDescriptor == null)   beanDescriptor=new BeanDescriptor(ODGDrawingPanelBeanInfo.class);
  beanDescriptor.setValue("isContainer",Boolean.FALSE);
  beanDescriptor.setDisplayName("DrawingPanel");
  return beanDescriptor;
}
