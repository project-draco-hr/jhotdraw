{
  StringBuilder buf=new StringBuilder();
  boolean first=true;
  for (  Ruleset r : rulesets) {
    if (first) {
      first=false;
    }
 else {
      buf.append('\n');
    }
    buf.append(r.toString());
  }
  return buf.toString();
}
