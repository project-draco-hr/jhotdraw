{
  for (  Map.Entry<Shape,List<Handle>> entry : shapes) {
    if (entry != null) {
      List<Handle> handles=entry.getValue();
      for (      Handle h : handles) {
        h.updateHandle.run();
      }
    }
  }
}
