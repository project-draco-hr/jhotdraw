{
  @SuppressWarnings("unchecked") StyleableMapAccessor<Object> k=(StyleableMapAccessor<Object>)findKey(element,attributeName);
  if (k == null) {
    return null;
  }
  if (!element.containsKey(StyleOrigin.USER,k)) {
    return INITIAL_VALUE_KEYWORD;
  }
  return k.getConverter().toString(element.get(k));
}
