{
  if (beanDescriptor == null)   beanDescriptor=new BeanDescriptor(ODGPropertiesPanelBeanInfo.class);
  beanDescriptor.setValue("isContainer",Boolean.FALSE);
  beanDescriptor.setDisplayName("ODGPropertiesPanel");
  return beanDescriptor;
}
