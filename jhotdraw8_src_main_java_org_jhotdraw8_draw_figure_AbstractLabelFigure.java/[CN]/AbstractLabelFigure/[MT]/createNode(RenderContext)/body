{
  Group g=new Group();
  g.setAutoSizeChildren(false);
  Region r=new Region();
  r.setScaleShape(true);
  g.getChildren().add(r);
  Text text=new Text();
  g.getChildren().add(text);
  return g;
}
