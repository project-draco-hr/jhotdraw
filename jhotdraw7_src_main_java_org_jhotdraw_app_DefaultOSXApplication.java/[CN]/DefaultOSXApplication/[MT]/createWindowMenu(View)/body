{
  ApplicationModel model=getModel();
  JMenu m;
  JMenuItem mi;
  m=new JMenu();
  JMenu windowMenu=m;
  labels.configureMenu(m,"window");
  m.addSeparator();
  if (view != null) {
    new WindowMenuHandler(windowMenu,view);
  }
  return m;
}
