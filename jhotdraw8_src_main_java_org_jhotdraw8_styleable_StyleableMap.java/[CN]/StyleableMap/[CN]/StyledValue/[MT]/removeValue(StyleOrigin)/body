{
  if (hasValue(origin)) {
    int i=origin.ordinal();
    @SuppressWarnings("unchecked") T oldValue=(T)setValue(i,NO_VALUE);
    if (this.origin == origin.ordinal()) {
      this.origin=-1;
      for (int j=origin.ordinal() - 1; j >= 0; j--) {
        if (getValue(j) != NO_VALUE) {
          this.origin=j;
          break;
        }
      }
    }
    return oldValue;
  }
 else {
    return null;
  }
}
