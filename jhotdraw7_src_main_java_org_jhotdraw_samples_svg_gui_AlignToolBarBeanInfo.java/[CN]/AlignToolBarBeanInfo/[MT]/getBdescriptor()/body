{
  if (beanDescriptor == null)   beanDescriptor=new BeanDescriptor(AlignToolBarBeanInfo.class);
  beanDescriptor.setValue("isContainer",Boolean.FALSE);
  beanDescriptor.setDisplayName("AlignToolPane");
  return beanDescriptor;
}
