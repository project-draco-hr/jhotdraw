{
  if (progressOwner == null || model.getOwner() == progressOwner) {
    models.add(model);
    model.addPropertyChangeListener(handler);
    updateProgressBar();
    updateToolTip();
    updateIndeterminate();
  }
}
