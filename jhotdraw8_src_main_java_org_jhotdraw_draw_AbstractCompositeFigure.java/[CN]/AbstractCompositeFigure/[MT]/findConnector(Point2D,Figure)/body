{
  ObservableList<Figure> cs=children();
  for (int i=cs.size() - 1; i >= 0; i--) {
    Figure c=cs.get(i);
    Connector cr=c.findConnector(p,prototype);
    if (cr != null) {
      return cr;
    }
  }
  return null;
}
