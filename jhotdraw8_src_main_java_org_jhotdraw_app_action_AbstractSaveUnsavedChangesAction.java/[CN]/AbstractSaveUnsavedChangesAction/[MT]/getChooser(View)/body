{
  @Nullable URIChooser chsr=view.getValue(SAVE_CHOOSER_KEY);
  if (chsr == null) {
    chsr=getApplication().getModel().createSaveChooser();
    view.putValue(SAVE_CHOOSER_KEY,chsr);
  }
  return chsr;
}
