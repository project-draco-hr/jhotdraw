{
  tt.nextToken();
  skipWhitespace(tt);
  while (tt.currentToken() != CssTokenizer.TT_EOF && tt.currentToken() != '{' && tt.currentToken() != ';') {
    tt.pushBack();
    parseComponentValue(tt);
    tt.nextToken();
  }
  tt.pushBack();
  parseCurlyBlock(tt);
  return null;
}
