{
  if (beanDescriptor == null)   beanDescriptor=new BeanDescriptor(ActionsToolBarBeanInfo.class);
  beanDescriptor.setValue("isContainer",Boolean.FALSE);
  beanDescriptor.setDisplayName("ActionToolPane");
  return beanDescriptor;
}
