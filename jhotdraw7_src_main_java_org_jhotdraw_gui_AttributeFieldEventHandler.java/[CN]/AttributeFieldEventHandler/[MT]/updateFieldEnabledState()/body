{
  if (getCurrentView() != null) {
    field.getComponent().setEnabled(getCurrentView().isEnabled() && getCurrentView().getSelectionCount() > 0);
  }
 else {
    field.getComponent().setEnabled(false);
  }
}
