{
  if (value == null) {
    out.append("none");
    return;
  }
  out.append('/');
  appendExpr(out,value.getFind());
  out.append('/');
  if (!"$1".equals(value.getReplace())) {
    appendExpr(out,value.getReplace());
    out.append('/');
  }
}
