{
  if (listeners != null) {
    for (    L l : new ArrayList<>(listeners)) {
      if ((l instanceof WeakListener) && ((WeakListener)l).wasGarbageCollected()) {
        removeListener(l);
      }
      consumer.accept(l);
    }
  }
}
