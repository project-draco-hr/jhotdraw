{
  if (beanDescriptor == null)   beanDescriptor=new BeanDescriptor(AbstractToolBarBeanInfo.class);
  beanDescriptor.setValue("isContainer",Boolean.FALSE);
  beanDescriptor.setDisplayName("AbstractToolBar");
  return beanDescriptor;
}
