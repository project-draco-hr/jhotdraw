{
  final CssSize top=topKey.get(a);
  final CssSize right=rightKey.get(a);
  final CssSize bottom=bottomKey.get(a);
  final CssSize left=leftKey.get(a);
  return new CssSizeInsets(top == null ? CssSize.ZERO : top,right == null ? CssSize.ZERO : right,bottom == null ? CssSize.ZERO : bottom,left == null ? CssSize.ZERO : left);
}
