{
  if (beanDescriptor == null)   beanDescriptor=new BeanDescriptor(LinkToolBarBeanInfo.class);
  beanDescriptor.setValue("isContainer",Boolean.FALSE);
  beanDescriptor.setDisplayName("LinkToolPane");
  return beanDescriptor;
}
