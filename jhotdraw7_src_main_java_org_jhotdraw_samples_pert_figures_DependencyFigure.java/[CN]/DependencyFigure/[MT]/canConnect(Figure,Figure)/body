{
  if ((start instanceof TaskFigure) && (end instanceof TaskFigure)) {
    TaskFigure sf=(TaskFigure)start;
    TaskFigure ef=(TaskFigure)end;
    if (ef.getPredecessors().contains(sf)) {
      return false;
    }
    return !sf.isDependentOf(ef);
  }
  return false;
}
