{
  int size=size();
  @SuppressWarnings("unchecked") T[] r=a.length >= size ? a : (T[])java.lang.reflect.Array.newInstance(a.getClass().getComponentType(),size);
  a=r;
  int i=0;
  for (Iterator<V> it=iterator(); it.hasNext(); ) {
    @SuppressWarnings("unchecked") T tmp=(T)it.next();
    a[i++]=tmp;
  }
  return a;
}
