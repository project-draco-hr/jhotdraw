{
  this.url=file.toURL();
  BufferedInputStream in=null;
  try {
    in=new BufferedInputStream(new FileInputStream(file));
    read(in,drawing);
  }
  finally {
    if (in != null) {
      in.close();
    }
  }
  this.url=null;
}
