{
  InputStream in=null;
  try {
    in=(InputStream)t.getTransferData(new DataFlavor("image/svg+xml","Image SVG"));
    return readFigures(in);
  }
  finally {
    if (in != null) {
      in.close();
    }
  }
}
