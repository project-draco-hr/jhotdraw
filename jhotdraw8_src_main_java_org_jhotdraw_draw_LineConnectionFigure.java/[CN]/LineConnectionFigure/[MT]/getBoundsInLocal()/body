{
  Point2D start=get(START);
  Point2D end=get(END);
  return new BoundingBox(min(start.getX(),end.getX()),min(start.getY(),end.getY()),abs(start.getX() - end.getX()),abs(start.getY() - end.getY()));
}
