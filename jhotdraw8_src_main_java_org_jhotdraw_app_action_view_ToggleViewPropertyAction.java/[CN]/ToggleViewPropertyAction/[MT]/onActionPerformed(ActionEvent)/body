{
  if (property != null)   property.set(!property.get());
 else {
    Node node=nodeGetter.apply(getActiveView());
    node.setVisible(!node.isVisible());
    this.set(SELECTED_KEY,node.isVisible());
  }
}
