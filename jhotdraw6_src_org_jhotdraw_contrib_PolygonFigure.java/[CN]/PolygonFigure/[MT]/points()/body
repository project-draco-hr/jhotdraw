{
  Vector pts=new Vector(pointCount());
  for (int i=0; i < pointCount(); ++i) {
    pts.addElement(new Point(getInternalPolygon().xpoints[i],getInternalPolygon().ypoints[i]));
  }
  return pts.elements();
}
