{
  Object[] v=formatter.fromString(buf);
  Set<String> l=new LinkedHashSet<>((int)v[0]);
  for (int i=0, n=(int)v[0]; i < n; i++) {
    l.add((String)v[i + 1]);
  }
  return new ImmutableObservableList<>(l);
}
