{
  if (beanDescriptor == null)   beanDescriptor=new BeanDescriptor(JSVGDrawingAppletPanelBeanInfo.class);
  beanDescriptor.setValue("isContainer",Boolean.FALSE);
  beanDescriptor.setDisplayName("SVGDrawingPanel for Applets");
  return beanDescriptor;
}
