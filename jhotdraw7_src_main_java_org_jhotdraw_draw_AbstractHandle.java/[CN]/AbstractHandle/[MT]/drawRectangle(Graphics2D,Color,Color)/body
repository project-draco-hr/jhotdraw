{
  Rectangle r=getBounds();
  if (stroke != null) {
    g.setStroke(new BasicStroke());
    g.setColor(stroke);
    g.draw(r);
  }
  if (fill != null) {
    g.setColor(fill);
    r.grow(-1,-1);
    g.fill(r);
  }
}
