{
  if (drawing != null) {
    Color color=showColorChooserDialog(this,labels.getString("attribute.backgroundColor"),drawing.get(CANVAS_FILL_COLOR));
    colorButton.setBackground(color);
    updateDrawing();
  }
}
