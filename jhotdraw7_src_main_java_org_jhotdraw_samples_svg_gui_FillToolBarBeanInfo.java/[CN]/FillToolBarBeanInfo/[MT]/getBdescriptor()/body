{
  if (beanDescriptor == null)   beanDescriptor=new BeanDescriptor(FillToolBarBeanInfo.class);
  beanDescriptor.setValue("isContainer",Boolean.FALSE);
  beanDescriptor.setDisplayName("FillToolBar");
  return beanDescriptor;
}
