{
  Graphics2D g=(Graphics2D)gr;
  super.paintIcon(c,g,x,y);
  Color color=(Color)editor.getDefaultAttribute(key);
  if (color == null) {
  }
 else {
    g.setColor(color);
    g.translate(x,y);
    g.fill(colorRect);
    g.translate(-x,-y);
  }
}
