{
  x-=width / 2;
  y-=height / 2;
  float r=(float)Math.sqrt(x * x + y * y);
  float theta=(float)Math.atan2(y,-x);
  float hue=(float)(0.5 + (theta / Math.PI / 2d));
  float[] hsb={hue,Math.min(1f,(float)r / getRadius()),brightness};
  return hsb;
}
