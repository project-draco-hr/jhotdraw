{
  super(w,h,null,0,w);
  pixels=new int[w * h];
  this.w=w;
  this.h=h;
  this.colorSystem=sys;
  generateLookupTables();
  newPixels(pixels,ColorModel.getRGBdefault(),0,w);
  setAnimated(true);
  generateColorWheel();
}
